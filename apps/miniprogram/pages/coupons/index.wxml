<view class="page">
  <view class="h">领券中心</view>
  <view wx:for="{{offers}}" wx:key="id">{{item.name}} <button size="mini" data-id="{{item.id}}" bindtap="claim">领取</button></view>
  <view class="h">我的券</view>
  <view wx:for="{{mine}}" wx:key="id">{{item.name}} {{item.status}}</view>
</view>
